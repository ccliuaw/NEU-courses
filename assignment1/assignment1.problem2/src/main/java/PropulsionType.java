/**
 * Represent the propulsion type of the vessel
 */
public enum PropulsionType {
  /**
   *  using sail power
   */
  sailPower,
  /**
   * using inboard engin
   */
  inboardEngin,
  /**
   * using outboard engin
   */
  outboardEngin,
  /**
   * using jet propulsion
   */
  jetPropulsion
}
